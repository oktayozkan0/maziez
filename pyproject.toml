[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "maziez"
version = "1.0.0"

dependencies = [
    "networkx >= 3.0",
]

[project.scripts]
solve = "maziez.__main__:main"